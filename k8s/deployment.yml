apiVersion: apps/v1
kind: Deployment
metadata:
  name: to-do
  namespace: to-do-app
spec:
  replicas: 4
  selector:
    matchLabels:
      app: to-do
  template:
    metadata:
      labels:
        app: to-do
    spec:
      containers:
      - name: to-do-app
        image: sharara99/to-do-list:latest  
        ports:
          - containerPort: 3000